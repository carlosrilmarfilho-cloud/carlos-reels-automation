name: TikTok pausado

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Pausado pelo usuário
        run: echo "Postagens automáticas no TikTok estão pausadas."
